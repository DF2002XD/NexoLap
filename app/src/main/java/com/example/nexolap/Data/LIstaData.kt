package com.example.nexolap.Data

/**
 * Represents a single item in a list, containing a title.
 *
 * This data class is used to model the data for each row in a list view,
 * holding the primary text content to be displayed.
 *
 * @property title The main text or title for this list item.
 */
data class ListaData(
    var title: String
)
